import { DesktopApiClient } from './types';

export class DesktopApiClientService implements DesktopApiClient {
  async printReceipt(receipt: unknown) {
    // Implementation to send print command to the desktop application
  }
}
