import { CustomerScreen } from './types';

export class CustomerScreenService implements CustomerScreen {
  reportStatus(status: string) {
    // Here you would implement the logic to report the status to your customer screen
  }
}
